import { makeStyles } from '@mui/material/styles';

const styles = () => {
    return {
        box: {

        }, 
        text: {

        }
    }
}

const useStyles = makeStyles(styles);
export default useStyles